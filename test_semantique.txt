BeginProject TestSem;
Setup:
    define x : integer;
    define x : float;
    const Limite : integer = 10;
    define tab : [integer; 3];
    define r : float;

Run:
{
    y <- 5;
    x <- "abc";
    Limite <- 7;
    tab[1.5] <- 2;
    tab[1] <- 3.2;
    r <- x + 2.5;
}
EndProject;
